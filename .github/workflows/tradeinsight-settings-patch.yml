name: Temporary TradeInsight Settings Patch

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: v18.9.2-tradeinsight-settings
          fetch-depth: 0
      - name: Placeholder
        run: echo 'Patch source retained in PR apply workflow history only.'
