'use strict';
/* DE.PULSE v18.6.1 canonical watchlist desk-key contract.
   The v18.5.1 watchlist extension is loaded as a separate classic script and
   therefore cannot depend on an undeclared identifier from renderer.js.
   Keep this list aligned with the three permanent trading desks. */
var DESKS = Object.freeze(['day', 'swing', 'long']);
