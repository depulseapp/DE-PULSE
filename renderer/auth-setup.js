'use strict';
const form=document.getElementById('setup-form'),button=document.getElementById('setup-button'),message=document.getElementById('auth-message');
function cookie(name){const hit=document.cookie.split('; ').find(x=>x.startsWith(name+'='));return hit?decodeURIComponent(hit.slice(name.length+1)):''}
form.addEventListener('submit',async e=>{e.preventDefault();message.textContent='';const password=document.getElementById('password').value,confirm=document.getElementById('confirm').value;if(password!==confirm){message.textContent='Passwords do not match.';return}if(password.length<12){message.textContent='Password must be at least 12 characters.';return}button.disabled=true;try{const r=await fetch('/api/auth/set-password',{method:'POST',headers:{'Content-Type':'application/json','X-DE-PULSE-CSRF':cookie('depulse_csrf')},body:JSON.stringify({password})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Unable to secure the Owner account.');location.replace('/')}catch(err){message.textContent=err.message;button.disabled=false}});
