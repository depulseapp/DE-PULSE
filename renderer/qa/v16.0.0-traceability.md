# DE.PULSE v16.0.0 TEST — Professional Truth Foundation Traceability

**Build:** `v16.0.0-test-professional-truth-foundation-20260811`  
**Protected baseline:** `v15.1.2 STABLE` remains frozen and untouched.  
**Rule:** This is explicitly a TEST build. It cannot be promoted by automated PASS alone.

| Audit item | Professional requirement | Runtime proof | UI proof | Behavioral proof | Status |
|---|---|---|---|---|:---:|
| #16 | Provider reconciliation uses independent observations, timestamps, Router winner and explicit conflict reason | `buildProviderReconciliation` + `providerQuotes` | Maintenance → Provider Reconciliation v16 | Router winner, conflict threshold, stale-observation tests | CLOSED |
| #17 | Corporate actions protect historical integrity; adjusted calculations + raw provenance + symbol lineage | expanded `CorporateAction`, `daily-raw`, `CorporateActionTruth` | Maintenance → Corporate Actions & Symbol Lifecycle v16 | split ratio, FB→META lineage, `adjustment=raw` tests | CLOSED |
| #25 | Research freshness is selected-ticker-specific; worst critical dependency wins | `ResearchPackageTruth` | Research → v16 Evidence Truth | global-News cannot satisfy target, missing quote blocks, conflict blocks | CLOSED |
| #30 | Prior-build audit is mandatory and tests runtime behavior rather than markers | `previous_build_audit_gate.py`, `v16_0_scope_gate.py` | QA history / build identity | v16 professional behavioral suite + inherited suite | CLOSED |

## Shared professional foundation

- Stable evidence fingerprint (`EvidenceSnapshot.ID`) changes only when material evidence changes; clock aging alone does not create a new snapshot.
- TEST uses `PersonalMarketTerminal-v16-TEST`, separate from the Stable runtime/config/instance lock.
- Provider conflicts are surfaced; no silent averaging.
- Canonical historical calculations remain adjusted; raw daily bars are retained separately for affected corporate-action symbols when Alpaca entitlement permits.
