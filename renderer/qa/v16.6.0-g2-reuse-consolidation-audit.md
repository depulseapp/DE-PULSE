# DE.PULSE v16.6.0 G2 Reuse / Consolidation Audit

**Master Symbol defect classification:** REFACTOR existing canonical desk-membership persistence.

- REUSE existing Day/Swing/Long watchlists, Master Symbol endpoints, public state and Provider Router.
- REFACTOR empty-list normalization so `nil` means legacy/missing while explicit `[]` means intentionally empty.
- ADD only a narrow bulk `/api/master-symbol/remove-all` action and UI control over the same canonical state.
- CONSOLIDATE Maintenance truth: user Master Symbol Store count is separate from protected System Market Context and VIX Special Index Store.
- No second symbol store, provider scheduler, deterministic engine or v16.7 feature path.
