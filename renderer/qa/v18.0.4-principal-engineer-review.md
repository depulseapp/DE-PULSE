# DE.PULSE v18.0.4 TEST — Principal Engineer Review

The v18.0.3 native run was useful evidence, not a release failure to bypass. macOS passed source tests and native packaging before a harness field-name assertion failed. Windows exposed SQLite lifecycle cleanup defects in real-Application tests plus the same harness assertion.

The v18.0.4 patch is deliberately bounded: persistence teardown is made deterministic for tests, and native release identity reads canonical `buildId`. Smart Router v2, Rapid Move, provider behavior, protected Day/Swing/Long formulas, No Execution, and v17.5.1 Stable isolation remain unchanged.

**PASS — GO to v18.0.4 pre-freeze/full regression and native G14/G15 recertification.**
