# DE.PULSE v16.1.1 — Verification Plan

Patch-only verification: reproduce the three escaped defects first; then run inherited v16.1 scope, professional/adversarial/Extreme, deterministic 2403/2403, full Go/vet, randomized x10, race x10, coverage, HTTP, renderer/content/version, responsive, exact-source/package integrity, provenance/SBOM and SHA-last verification.

Stable-promotion-only prerequisites remain supported release toolchain, govulncheck, and physical native target-host acceptance. They do not run in application startup.
