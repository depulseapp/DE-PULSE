# DE.PULSE v16.4.0 G1 Architecture / Risk Contract

| Requirement | Canonical owner | Source truth | UI | Degraded state | Deterministic impact |
|---|---|---|---|---|---|
| #22 AI Evidence Architecture v2 | `ai_research_v2.go` derived package over existing Evidence/Research/Market/Event snapshots | canonical evidence IDs/timestamps | Research AI result + grounding metadata | missing/partial evidence listed explicitly | NONE |
| #23 External-content safety | privileged `aiSystemPrompt` + evidence `Untrusted` boundary + response sanitizer | News/SEC/web/provider text remains data | safety warning/limitations | instruction-like content flagged; never privileged | NONE |
| #24 Routing/cost control | `GenerateAI` + `resolveAIRouting` + material package cache | configured provider keys/settings | Settings Routing & Cost Policy + response route/cache metadata | no configured route = unavailable/error, never silent success | NONE |

Hard invariants: external text cannot become instructions; only supplied evidence IDs can be cited; Setup Score is not win probability; manual routing never silently falls back; ordinary quote ticks do not trigger deep reanalysis; AI cannot mutate Score/Action, Market Tradeability, Trade Readiness, execution, or membership.
