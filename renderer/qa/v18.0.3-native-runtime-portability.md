# DE.PULSE v18.0.3 TEST — Native Runtime Portability Hardening

- **Build ID:** `v18.0.3-test-native-cross-platform-runtime-portability-hardening-20260813`
- **Runtime:** `PersonalMarketTerminal-v18.0.3-TEST`
- **Bundle:** `De-Pulse-v18.0.3-TEST.app`
- **Stable baseline:** v17.5.1

## G14 findings closed

1. Embedded renderer/auth paths now use platform-neutral embedded-FS path semantics, preventing Windows `\` normalization from causing setup/login/static 404s.
2. Tests that construct a real Application isolate Linux/macOS/Windows user-config roots consistently and derive `os.UserConfigDir()` instead of assuming one filesystem layout.
3. Unix `0600` assertions are not misapplied to Windows `os.FileMode`; Windows isolation/ACL truth remains a native-delivery responsibility.
4. Windows G14 uses explicit PowerShell `-LiteralPath` / `-Value` binding for the Stable sentinel.
5. v17.5.1 Stable remains read-only and isolated during TEST migration.

No market-intelligence decision logic changed. Native G14/G15 must pass before promotion.
