# DE.PULSE v16.3.0 — G2 Source Health / Reuse & Consolidation Audit

**Decision order:** REUSE → CONSOLIDATE → REFACTOR → DELETE/REPLACE → ADD.

## Baseline reuse decisions

| Capability | G2 decision | Result |
|---|---|---|
| Signal snapshot/outcome structures | **REFACTOR** | Existing `SignalValidationState` retained and expanded; no parallel validation store. |
| Evidence identity | **REUSE** | Existing Evidence Snapshot ID is frozen into v16.3 signal snapshots. |
| Day/Swing/Long deterministic engine | **REFACTOR / REUSE** | Shared scoring primitive extracted without changing math; live and replay paths use the same deterministic implementation. |
| Historical bars | **REUSE** | Existing `Engine.bars` remains sole history store. SPY/QQQ daily depth is selectively extended through the same provider route. |
| Corporate-action adjustment | **REUSE** | Existing corporate-action truth remains the sole lifecycle/adjustment authority; outcome normalization and replay refusal use it. |
| Market Intelligence | **REUSE** | Existing v16.1 snapshot is context only; no duplicate market-intelligence calculation. |
| Event Intelligence | **REUSE** | Existing v16.2 snapshot is frozen as context only; no duplicate event provider/fetch path. |
| Decision Queue / candidate context | **REFACTOR** | Correlation may raise attention priority only; it does not change opportunity quality. |
| Seasonality | **ADD** | New derived analytics only; no new store/provider owner. |
| Calibration | **ADD** | Derived from Signal Validation outcomes only. |
| Replay orchestration | **ADD** | Isolated cutoff view around the same deterministic engine; no replay-only scoring formulas. |
| Correlation / concentration | **ADD** | Derived aligned-return context only. |

## Current source-health metrics

- production Go files: **45**;
- production Go lines: **19,397**;
- largest production Go module: `http_api.go` **1,092 lines**, below the 1,200-line module budget;
- `validation_learning.go`: **756 lines**;
- orphan production Go helpers: **0**;
- orphan named renderer helpers: **0**;
- exact duplicate sizeable Go bodies: **0**.

`source_health_architecture_gate.py` result: **PASS**.

## Developer-proof conclusion

The v16.3 scope adds one responsibility-focused analytics module and refactors existing owners instead of appending parallel engines/stores. Canonical data ownership, Provider Router and deterministic formulas remain unchanged. The former v16 TEST runtime isolation is intentionally retired for Stable promotion; canonical `PersonalMarketTerminal` runtime continuity now preserves compatible v15.1.2-and-earlier settings and API credentials.
