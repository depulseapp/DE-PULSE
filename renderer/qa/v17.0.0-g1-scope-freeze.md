# DE.PULSE v17 — G1 Immutable Major Scope

The v17 major scope is frozen in `v17_major_scope.json` at 20 approved items. v17.0.0 is a TEST delivery slice and must not silently drop later v17 obligations.

## v17.0.0 implementation pulled forward

This slice now includes more than the initial persistence skeleton: repository abstraction, SQLite schema/migrations on the native host path, Global Symbol Registry, canonical warm start, material-change async persistence, structured evidence/decision/outcome/feature foundations, shared workload queues, provider request telemetry, load shedding, degradation reasons, runtime profiling and SLO assessment.

## Open issue disposition

- **OPEN-003 Runtime overload / DATA DEGRADED / slow response:** active v17 architecture priority; Stable blocker if critical live/current truth can be delayed or misstated.
- **OPEN-001 Master Market Symbols layout:** NEXT COMPATIBLE v17.x UI/hardening unless G0 evidence elevates it.
- **OPEN-002 Prep exception-list repetition:** NEXT COMPATIBLE v17.x; escalate if backend truth proves materially misleading.

v18 authentication/multi-user/session/quota scope remains excluded.
