# DE.PULSE v18.4.0 — G2 Architecture / Data Utility

Status: PASS

Canonical ownership remains singular. HTTP request/cookie/origin policy belongs to the HTTP security/auth boundary; session truth belongs to IdentityService; provider entitlement/data-rights truth belongs beside provider capability/routing metadata; security/commercial telemetry reuses canonical runtime observability. v18.4 must not create another PostgreSQL, market-data, scanner, scoring or intelligence pipeline.

Security state is retained only when it has a consumer and bounded purpose. Abuse controls must avoid persistent raw credential/client-identity data and must use bounded memory/retention. Provider rights metadata must be explicit, reviewable and consumed by routing/commercial-readiness decisions rather than displayed as raw clutter.
