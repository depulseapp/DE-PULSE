# CURRENT Adaptive Build Process

**Canonical machine state:** `governance/current-state.json`  
**Certified Stable:** `v18.10.0` — immutable  
**Retained process-control authority (historical COMPLETE):** #107 / `ADAPT-PROVIDER-PROFESSIONAL-CLOSURE-001` / `adapt-provider-professional-closure-001`  
**Completed post-v18 audit:** #145 / PR #146 — **PASS**  
**Future hosted umbrella:** #66 — `PLANNED_UNSTARTED`; v19 G1 is permitted but not started.

The permanent execution loop remains source-driven and exact-head: LOOKUP -> COMPARE -> CLASSIFY -> DECIDE -> UPDATE -> Fast -> Qualified -> G11–G16 when a release is actually being produced. v18.10.0 remains immutable.

The inherited Data Health process remains in force: **#81/#82/#83/#78/#84** continue to govern Router adoption, common health/recovery, lifecycle, TradeInsight admission and zero-gap closure. Unclassified provider/fetch paths fail closed, and **canonical freshness** remains authoritative.

## Rebaselined process rules

1. Start from live source, not roadmap labels. Before G1 implementation, classify each requirement against current canonical owners as `REUSE`, `EXTEND_EXISTING`, `CONSOLIDATE`, `JUSTIFIED_RESIDUAL`, or `NOT_APPLICABLE`.
2. Bind evidence while coding. Development is not complete until canonical owner, consumer, positive/adverse evidence and persistence/security/UI applicability are recorded.
3. Classify assurance gaps correctly: product behavior gap vs test/evidence gap vs ownership-binding gap vs legitimate N/A.
4. One coherent candidate per coherent correction. A changed candidate still requires fresh exact-head Fast; efficiency comes from reducing needless candidate SHAs, not suppressing validation.
5. Requirements are not releases. `HOST-001..072` remain traceability rows; implement dependency-coherent bands/slices.
6. Reuse canonical runtime owners: Smart Provider Router v2, Data Health/freshness, cache/persistence, multi-feed subscription, telemetry/reconciliation/lifecycle, identity/session, Research/Discovery, decision/outcome lineage and direct SEC/EDGAR.
7. Hosted threat modeling begins before development exit: tenant escape, cache/coalescing leakage, long-lived stream revocation, secret leakage, mixed-client downgrade, noisy-neighbor pressure and backup/restore isolation fail closed.
8. Point-in-time before learning. Historical/adaptive evaluations reconstruct what was genuinely knowable at the decision timestamp.
9. Measure usefulness, not just availability. Intelligence slices name their outcome/calibration signal and reuse canonical decision/outcome history.
10. Trader-grade adverse state explicitly distinguishes exchange halt/LULD/volatility pause/resume from provider outage, stale quote and closed-session state.
11. Historical CI is conserved, not multiplied. Frozen v18 T1–T10 remain durable baseline evidence; deeper historical logic is impact-triggered while changed product/regression tests remain fully enforced.
12. Post-merge continuity is not PR Fast. Main-push continuity/branch hygiene remains operationally distinct from PR validation.

## Exactly one next action

Reserve the first coherent v19 G1 **Hosted Trust Foundation** work slice, enumerate the selected HOST rows and exact canonical owners/residuals, and only then begin implementation.
